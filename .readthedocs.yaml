version: 2

build:
  os: "ubuntu-22.04"
  tools:
    python: "3.11"

python:
  install:
    - method: pip
      path: .
  system_packages: true

# Build from the docs/ directory with Sphinx
sphinx:
  configuration: docs/conf.py
